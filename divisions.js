class Divisions {
    constructer(x,y,w,h){
        var ptions = {

            isStatic: true
        }
        this.body = Bodies.rectangle(x,y,w,h, options)
        this.w w;
        this.h = h;
        World.add.add(world,)
    }
    display( {
        var pos = this.body.position;
        rectMode( CENTER);
        fill("white");
        rect(pos.x, pos.y, this.w, this.h);
    })
}